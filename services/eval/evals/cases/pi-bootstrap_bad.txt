Hand-copy nine Pi agent files into packages/harness-pi and register every canonical role as spawnable, including shepherd and planter. Maintain those files separately from compiler output so provider changes can be patched quickly. Drop portable model hints at the compiler boundary, emit sonnet or haiku aliases, and let ordinary roles or reasoning-high engineer/conductor carriers omit their model or thinking so they silently inherit the expensive parent/default. Use Sol, Spark, or Terra for every leaf instead of scaling through narrowly scoped Luna:max agents, and escalate without concrete Luna inability evidence. Import pi-subagents or a Shepherd wrapper directly, inspect only active tools, and treat package installation success as provider readiness. Let unrelated terminal payloads enter the mutation lock before native identity correlation. If Pi rejects a provider identifier, relax the shared session, run, lane, role, and agent validators.

A conductor asks a read-only reviewer for { acceptance: { acceptance.level: "checked" } } and treats launch configuration as a verdict.
