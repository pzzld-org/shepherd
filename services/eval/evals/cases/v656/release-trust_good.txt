Release readiness is one measured compatibility and ownership decision. Read the release version from tracked authorities and report the native shepherd-cli, `fl03:shepherd` Component contract, all four npm package manifests, and staged Claude, Codex, and Pi carriers at the same exact version. Ignore untracked, gitignored `.pi/tasks/current.json` runtime churn, but fail if the same version literal appears in future tracked `.pi` source without classification.

Current install, marketplace, repository, badge, and runtime URLs use `pzzld-org/shepherd`; historical changelog text, contributor credit to FL03, issue attribution, CODEOWNERS, and the personal funding account remain unchanged. Shared `.claude/settings.json` carries only project-required plugin discovery. It grants no bypass mode or `Bash(*)`, suppresses no safety prompt, and contains no personal model, UX, experimental-team, memory, dream, or workflow automation preference.

The release publishes a `SECURITY.md` contract with supported versions, private reporting, response stages, coordinated disclosure, scope, and safe harbor. Dependabot owns GitHub Actions, npm, and Cargo. The committed dependency report records the measured npm and Cargo commands and classifies every observed finding by ecosystem, severity, affected range, fixed version availability, production closure, dependency path, reachability, and shipped artifact. Lockfile-derived production closure must agree with that classification. A reachable shipped high or critical finding fails unless its waiver names an owner, concrete reason, tracking URL, and future expiry. The current npm critical is build-only under JCO, absent from every shipped package closure, and therefore classified without a waiver. No dependency changes are made because `npm outdated` found none and no reachable production high or critical had a measured upgrade.

The release workflow runs the compatibility report and deterministic dependency gate before publication. Run `cargo deny` when it is installed; if it is unavailable, report the missing gate instead of inventing a passing result. Synthetic controls prove ignored runtime state passes, tracked unclassified source fails, bypass settings fail, an unowned finding fails, a reachable unwaived finding fails, and an expired waiver fails.
