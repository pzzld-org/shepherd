At the Pi adapter boundary, map each opaque provider tool-call identifier to a fixed-length SHA-256 correlation token before calling the typed Shepherd component. Compare the native response to that same token. Keep project, session, run, lane, role, and agent identifiers on their existing strict validators. Use Node's standard library, reject missing identifiers, add no dependency, and prove the original call_*|fc_* failure plus mismatched-response and sibling-identity negatives.
