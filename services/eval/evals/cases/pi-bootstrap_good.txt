Generate Pi subagent definitions only in the staged package from the Rust compiler-owned Pi manifest and role prompts. Select exactly the seven roles whose canonical contract says dispatchable: true, preserve literal names such as shepherd:engineer, and keep shepherd plus planter as top-level non-spawnable identities. Declare the generated agents directory through Pi's measured pi.subagents.agents package contract and include it in npm's files allowlist; never track the generated tree.

Map the compiler's dispatch capability to the registered subagent tool. At root and child session_start use Pi's public getAllTools inventory, accept any compatible configured tool named exactly subagent even when inactive, and keep reads available while Write/Edit/Bash fail closed with one exact install-and-restart remediation if the inventory is absent or malformed. Give only dispatch-capable roles the minimum nested depth needed for engineer to run auditor and discovery concurrently and then critic. Prove a clean staged package contains nine canonical prompts and seven agents, a fresh isolated Pi process lists exactly those seven names, a provider-free fresh process blocks a real write attempt with the exact remediation, and a real nested engineer wave returns evidence from auditor, discovery, and critic. Keep Rust Component policy, strict sibling identifier validation, exact terminal correlation before mutation lockout, and adapter-boundary tool-call normalization unchanged.
