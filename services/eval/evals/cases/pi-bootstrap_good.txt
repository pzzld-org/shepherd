Generate Pi subagent definitions only in the staged package from the Rust compiler-owned Pi manifest and role prompts. Select exactly the seven roles whose canonical contract says dispatchable: true, preserve literal names such as shepherd:engineer, and keep shepherd plus planter as top-level non-spawnable identities. Declare the generated agents directory through Pi's measured pi.subagents.agents package contract and include it in npm's files allowlist; never track the generated tree.

Map the compiler's dispatch capability to the registered subagent tool. At root and child session_start use Pi's public getAllTools inventory, accept any compatible configured tool named exactly subagent even when inactive, and keep reads available while Write/Edit/Bash fail closed with one exact install-and-restart remediation if the inventory is absent or malformed. Register nested transport without granting dispatch policy, which remains in Component canonical capabilities. Preserve the authored portable model hint through the compiler, Component, WIT, and JSON role contract. Resolve Pi's closed config map to concrete `provider/model:thinking` values: reasoning leads use Sol at xhigh, while narrowly scoped critic, coder, auditor, worker, and discovery leaves use Luna at max. Pass that exact result as each generic subagent call's per-run model. Missing model or thinking fails closed. Project-neutral carriers use `inherit` only for inherit-caller and the impossible `model-required/model-required` sentinel otherwise, including reasoning-high engineer and conductor carriers, so direct launches cannot silently inherit. Escalate one bounded Luna task to Sol only after Luna returns concrete inability evidence.

Prove a clean staged package contains nine canonical prompts and seven agents, omitted worker model fails before child execution, an explicit Luna:max worker succeeds, and a Sol:xhigh conductor dispatches Luna:max worker and auditor children. Session metadata must show Luna leaves, the Sol conductor, no Spark, Terra, or Sol leaf, and native stopped revision 2 records. Keep Rust Component policy, strict sibling identifier validation, exact terminal correlation before mutation lockout, and adapter-boundary tool-call normalization unchanged.
