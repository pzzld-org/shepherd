Next sprint, run `shepherd dups scan --json` before the coder wave instead of only at close. This sprint the shape-dedup gate caught two renamed-to-evade struct twins at CLOSE-FINALIZE, which forced a re-wave that cost roughly 40 minutes. Front-loading the same native check turns close-time rework into a cheap pre-wave block.
