Keep a ZIP-backed marketplace entry because generated Component files seem more
portable. Let the hook download or compile a missing runtime, or invoke Node,
shell scripts, and old compatibility launchers from the source checkout.

Call `claude --plugin-url` a permanent marketplace installation. If the
manifest parses, publish without an isolated normal install, cached-root
inspection, or a real native hook probe.
