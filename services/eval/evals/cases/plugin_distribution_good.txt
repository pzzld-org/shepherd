Publish a normal Claude marketplace catalog with `source: "./plugins/shepherd"`.
The carrier has a byte-identical projection of the
canonical `.claude-plugin/plugin.json` and tracked relative links to
`hooks/hooks.json`, compiler-owned roles, and skills. Claude dereferences those
links into its installed cache.
Every registered hook uses Claude exec form:
`{"type":"command","command":"shepherd","args":["claude-hook"]}`.

The native `shepherd` executable must resolve from PATH. Its Rust bridge owns
identity normalization, lifecycle planning and persistence, and guard policy.
Do not include a plugin ZIP, generated Component runtime, Node hook, shell
hook, compatibility launcher, or a second policy implementation.

Before publication, strictly validate a dereferenced carrier fixture and run an
isolated install of the actual thin carrier. Inspect the cached root for a real
manifest, hooks, agents, and skills; reject escaping symlinks, `package.json`,
lockfiles, and `node_modules`; run the installer with Node/npm sentinels; and
execute its native PreToolUse hook with an unbound identity to prove the Claude
denial is fail-closed.

Publish Codex from `.agents/plugins/marketplace.json` with the same local source.
Codex skips symlinks, so its `.codex-plugin/plugin.json` selects a generated,
byte-gated regular projection under `plugins/shepherd/codex/`. Its hooks use one
command string, `shepherd codex-hook`. From isolated HOME and CODEX_HOME, add the
marketplace, install `shepherd@shepherd`, inspect the cache, and execute both a
SessionStart and fail-closed PreToolUse. Codex subagent lifecycle is not claimed
without trusted spawn-to-child correlation. No Node, npm, or Wasm exists in the
installed carrier.
