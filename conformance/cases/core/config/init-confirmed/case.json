{
  "suite": "core",
  "kind": "mutating",
  "authority": "native-v6.4.5",
  "intentional_difference": "native config init publishes the canonical layout-v5 document atomically and never derives legacy Poetry or harness configuration",
  "requires_git": true,
  "description": "config init confirms and captures the canonical document",
  "args": ["config", "init", "--confirm"],
  "db_fixture": "none",
  "capture_files": ["shepherd.toml"]
}
