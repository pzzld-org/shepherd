{
  "suite": "core",
  "kind": "mutating",
  "authority": "native-v6.4.6",
  "intentional_difference": "native lock reap probes the recorded PID and removes a dead-holder lock in the canonical layout; this saturating timestamp fixture keeps the age bucket deterministic while live recent holders remain protected",
  "requires_git": true,
  "description": "lock reap removes an old lock whose recorded holder is not alive",
  "args": ["lock", "reap"],
  "db_fixture": "full_schema",
  "seed_sql": [
    "INSERT INTO projects (id, name, created_at, updated_at) VALUES ('project-lock-reap', 'lock reap fixture', 0, 0)"
  ],
  "input_files": {
    ".shepherd/shepherd.lock": "stale-lock.json"
  },
  "capture_files": ["shepherd.lock"]
}
