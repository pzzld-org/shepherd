{
  "suite": "core",
  "kind": "mutating",
  "authority": "native-v6.4.6",
  "intentional_difference": "native lock release removes only the canonical project lock and closes its locks_history row; it does not touch legacy lock files or shell state",
  "requires_git": true,
  "description": "lock release removes the lock acquired by the setup step",
  "setup": [["lock", "acquire", "--mode", "parallel", "--session", "proof-release"]],
  "args": ["lock", "release"],
  "db_fixture": "full_schema",
  "seed_sql": [
    "INSERT INTO projects (id, name, created_at, updated_at) VALUES ('project-lock-release', 'lock release fixture', 0, 0)"
  ],
  "capture_files": ["shepherd.lock"]
}
