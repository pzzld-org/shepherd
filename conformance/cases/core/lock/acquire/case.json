{
  "suite": "core",
  "kind": "mutating",
  "authority": "native-v6.4.5",
  "intentional_difference": "native lock acquire creates the canonical descriptor-safe project lock and records the holder in locks_history without invoking legacy shell or Python coordination",
  "requires_git": true,
  "description": "lock acquire obtains a named native registry lock",
  "args": ["lock", "acquire", "--mode", "parallel", "--session", "proof-acquire"],
  "db_fixture": "full_schema",
  "seed_sql": [
    "INSERT INTO projects (id, name, created_at, updated_at) VALUES ('project-lock-acquire', 'lock acquire fixture', 0, 0)"
  ]
}
