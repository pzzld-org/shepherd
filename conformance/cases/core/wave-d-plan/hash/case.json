{
  "suite": "core",
  "kind": "pure",
  "authority": "native-v6.4.6",
  "intentional_difference": "legacy plan hash accepts an arbitrary retired plan path and persists legacy plan metadata; native v6.4.6 reads only the active run canonical plan and emits its SDK SHA-256 without mutation",
  "requires_git": true,
  "description": "native plan hash is bounded to one canonical run plan",
  "setup": [["init", "--confirm"], ["run", "init", "v900"]],
  "args": ["plan", "hash", "--run", "v900"],
  "db_fixture": "none",
  "input_files": {".shepherd/runs/v900/plan.md": "plan.md"}
}
