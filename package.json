{
  "name": "shepherd-workspace",
  "version": "6.4.5",
  "private": true,
  "description": "npm workspace root for the shepherd content compiler and the three harness adapters (packages/*). Not published itself -- the Rust workspace at Cargo.toml (crates/*, `[workspace.package].version`) is the single source of truth every package.json version here tracks.",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20",
    "npm": ">=10.3.0"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/FL03/shepherd.git"
  }
}
