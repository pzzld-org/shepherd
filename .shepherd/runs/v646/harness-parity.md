# Harness parity — event x harness x implementation

GENERATED FILE. Do not hand-edit. Regenerate with:

    bash hooks/scripts/generate_harness_parity.sh

Ground truth: `hooks/hooks.json` (Claude), `plugins/shepherd/codex/hooks/hooks.json`
(Codex), `packages/harness-pi/shepherd.pi.json` (Pi). Pi dispatches in-process
through `packages/harness-pi/src/extension.mjs` rather than spawning a binary
per event; its manifest's `canonicalEvent` field is what maps it onto the
same event axis Claude and Codex use.

Three states, one per cell, never blank:

1. **Implemented and effective** — the event is handled and does real work.
2. **Registered but inert** — the event is registered but the handler has no
   observable effect; reason and citation given.
3. **Unsupported (harness limitation)** — the harness or host provides no
   support for this event/behavior; reason and citation given.

| Event | Claude | Codex | Pi |
|---|---|---|---|
| SessionStart | **Implemented and effective** — native adapter (`shepherd claude-hook`) resolves this event directly (see `crates/cli/src/cmd/claude_hook.rs`) | **Implemented and effective** — native adapter (`shepherd codex-hook`) resolves this event directly (see `crates/cli/src/cmd/native_hook.rs`) | **Implemented and effective** — in-process handler `src/extension.mjs:31 pi.on("session_start", ...)`: normalizes identity, plans lifecycle, calls shepherd dispatch bind-root; throws (fail-closed) if the component is unavailable (see `packages/harness-pi/src/extension.mjs`) |
| PreToolUse | **Implemented and effective** — native adapter (`shepherd claude-hook`) plus shell telemetry: hooks/scripts/seed_preflight_check.sh (see `crates/cli/src/cmd/claude_hook.rs; hooks/scripts/seed_preflight_check.sh`) | **Implemented and effective** — native adapter (`shepherd codex-hook`) resolves this event directly (see `crates/cli/src/cmd/native_hook.rs`) | **Implemented and effective** — in-process handler `src/extension.mjs:59 pi.on("tool_call", ...)`: calls shepherd dispatch resolve then guardWithComponent; returns {block: true, reason} on every error path (fail-closed) (see `packages/harness-pi/src/extension.mjs`) |
| PostToolUse | **Implemented and effective** — shell telemetry: hooks/scripts/agent_insight_capture.sh, hooks/scripts/bash_post.sh, hooks/scripts/discovery_capture.sh (no native `shepherd claude-hook` registration for this event) (see `hooks/scripts/agent_insight_capture.sh, hooks/scripts/bash_post.sh, hooks/scripts/discovery_capture.sh`) | **Unsupported (harness limitation)** — manifest declares no hook for this event (see `plugins/shepherd/codex/hooks/hooks.json`) | **Unsupported (harness limitation)** — manifest declares no hook for this canonical event (see `packages/harness-pi/shepherd.pi.json`) |
| SubagentStart | **Implemented and effective** — native adapter (`shepherd claude-hook`) binds subagent dispatch identity and role, and WRITES a dispatch record via `DispatchRequest::Start` as of commit 7d5492e — do not read this as inert, that was true earlier and is now stale. (see `crates/cli/src/cmd/native_hook.rs`) | **Unsupported (harness limitation)** — Codex exposes no trusted spawn-to-child correlation; binding an identity here would mint a shadow identity for a call the host never actually attributes to a subagent. This is a correctness decision, not a gap to fill. Pinned by crates/cli/tests/codex_hook_cli.rs:223,257. (see `crates/cli/src/cmd/native_hook.rs:229`) | **Unsupported (harness limitation)** — manifest declares no hook for this canonical event (see `packages/harness-pi/shepherd.pi.json`) |
| SubagentStart — write-scope narrowing | **Unsupported (harness limitation)** — Identity binding and role enforcement work for a dispatched subagent on Claude, but the write scope is always recorded as `**` because no host ever declares a narrower one; narrowing still requires a declared scope the host does not send. (see `crates/cli/src/cmd/native_hook.rs:358,390`) | **Unsupported (harness limitation)** — SubagentStart is refused outright on Codex (see the SubagentStart/SubagentStop row above), so no dispatch binding — and therefore no write scope — is ever constructed to narrow. (see `crates/cli/src/cmd/native_hook.rs:229`) | **Unsupported (harness limitation)** — The Pi manifest's hooks block declares no SubagentStart-equivalent event (only session_start, tool_call, session_shutdown), so there is no dispatch binding to narrow a write scope for. (see `packages/harness-pi/shepherd.pi.json`) |
| SubagentStop | **Implemented and effective** — native adapter (`shepherd claude-hook`) plus shell telemetry: hooks/scripts/subagent_telemetry.sh (see `crates/cli/src/cmd/claude_hook.rs; hooks/scripts/subagent_telemetry.sh`) | **Unsupported (harness limitation)** — Codex exposes no trusted spawn-to-child correlation; binding an identity here would mint a shadow identity for a call the host never actually attributes to a subagent. This is a correctness decision, not a gap to fill. Pinned by crates/cli/tests/codex_hook_cli.rs:223,257. (see `crates/cli/src/cmd/native_hook.rs:229`) | **Unsupported (harness limitation)** — manifest declares no hook for this canonical event (see `packages/harness-pi/shepherd.pi.json`) |
| CwdChanged | **Implemented and effective** — shell telemetry (`hooks/scripts/cwd_changed.sh`) performs real work; no native handler exists for this event: portable.rs types every unlisted event as `NativeEvent::Other`, which maps to `DispatchPlan::Ignored` — a native registration here would be a silent no-op, so the shell registration is the only place this behavior can live (see `crates/core/src/dispatch/portable.rs:810`) | **Unsupported (harness limitation)** — manifest declares no hook for this event (see `plugins/shepherd/codex/hooks/hooks.json`) | **Unsupported (harness limitation)** — manifest declares no hook for this canonical event (see `packages/harness-pi/shepherd.pi.json`) |
| PreCompact | **Implemented and effective** — shell telemetry (`hooks/scripts/precompact_snapshot.sh`) performs real work; no native handler exists for this event: portable.rs types every unlisted event as `NativeEvent::Other`, which maps to `DispatchPlan::Ignored` — a native registration here would be a silent no-op, so the shell registration is the only place this behavior can live (see `crates/core/src/dispatch/portable.rs:810`) | **Unsupported (harness limitation)** — manifest declares no hook for this event (see `plugins/shepherd/codex/hooks/hooks.json`) | **Unsupported (harness limitation)** — manifest declares no hook for this canonical event (see `packages/harness-pi/shepherd.pi.json`) |
| SessionShutdown | **Unsupported (harness limitation)** — manifest declares no hook for this event (see `hooks/hooks.json`) | **Unsupported (harness limitation)** — manifest declares no hook for this event (see `plugins/shepherd/codex/hooks/hooks.json`) | **Registered but inert** — declared for lifecycle parity across the three harnesses; the handler performs no action (`src/extension.mjs:58 pi.on("session_shutdown", ...)`: no-op, declared for lifecycle parity with the other two harnesses) — see this script's header comment for why intentional inertness is still state 2, not state 1 (see `packages/harness-pi/shepherd.pi.json`) |

## Every non-"Implemented and effective" cell, with its citation

- **PostToolUse / Codex** — **Unsupported (harness limitation)** — manifest declares no hook for this event (see `plugins/shepherd/codex/hooks/hooks.json`)
- **PostToolUse / Pi** — **Unsupported (harness limitation)** — manifest declares no hook for this canonical event (see `packages/harness-pi/shepherd.pi.json`)
- **SubagentStart / Codex** — **Unsupported (harness limitation)** — Codex exposes no trusted spawn-to-child correlation; binding an identity here would mint a shadow identity for a call the host never actually attributes to a subagent. This is a correctness decision, not a gap to fill. Pinned by crates/cli/tests/codex_hook_cli.rs:223,257. (see `crates/cli/src/cmd/native_hook.rs:229`)
- **SubagentStart / Pi** — **Unsupported (harness limitation)** — manifest declares no hook for this canonical event (see `packages/harness-pi/shepherd.pi.json`)
- **SubagentStart — write-scope narrowing / Claude** — **Unsupported (harness limitation)** — Identity binding and role enforcement work for a dispatched subagent on Claude, but the write scope is always recorded as `**` because no host ever declares a narrower one; narrowing still requires a declared scope the host does not send. (see `crates/cli/src/cmd/native_hook.rs:358,390`)
- **SubagentStart — write-scope narrowing / Codex** — **Unsupported (harness limitation)** — SubagentStart is refused outright on Codex (see the SubagentStart/SubagentStop row above), so no dispatch binding — and therefore no write scope — is ever constructed to narrow. (see `crates/cli/src/cmd/native_hook.rs:229`)
- **SubagentStart — write-scope narrowing / Pi** — **Unsupported (harness limitation)** — The Pi manifest's hooks block declares no SubagentStart-equivalent event (only session_start, tool_call, session_shutdown), so there is no dispatch binding to narrow a write scope for. (see `packages/harness-pi/shepherd.pi.json`)
- **SubagentStop / Codex** — **Unsupported (harness limitation)** — Codex exposes no trusted spawn-to-child correlation; binding an identity here would mint a shadow identity for a call the host never actually attributes to a subagent. This is a correctness decision, not a gap to fill. Pinned by crates/cli/tests/codex_hook_cli.rs:223,257. (see `crates/cli/src/cmd/native_hook.rs:229`)
- **SubagentStop / Pi** — **Unsupported (harness limitation)** — manifest declares no hook for this canonical event (see `packages/harness-pi/shepherd.pi.json`)
- **CwdChanged / Codex** — **Unsupported (harness limitation)** — manifest declares no hook for this event (see `plugins/shepherd/codex/hooks/hooks.json`)
- **CwdChanged / Pi** — **Unsupported (harness limitation)** — manifest declares no hook for this canonical event (see `packages/harness-pi/shepherd.pi.json`)
- **PreCompact / Codex** — **Unsupported (harness limitation)** — manifest declares no hook for this event (see `plugins/shepherd/codex/hooks/hooks.json`)
- **PreCompact / Pi** — **Unsupported (harness limitation)** — manifest declares no hook for this canonical event (see `packages/harness-pi/shepherd.pi.json`)
- **SessionShutdown / Claude** — **Unsupported (harness limitation)** — manifest declares no hook for this event (see `hooks/hooks.json`)
- **SessionShutdown / Codex** — **Unsupported (harness limitation)** — manifest declares no hook for this event (see `plugins/shepherd/codex/hooks/hooks.json`)
- **SessionShutdown / Pi** — **Registered but inert** — declared for lifecycle parity across the three harnesses; the handler performs no action (`src/extension.mjs:58 pi.on("session_shutdown", ...)`: no-op, declared for lifecycle parity with the other two harnesses) — see this script's header comment for why intentional inertness is still state 2, not state 1 (see `packages/harness-pi/shepherd.pi.json`)
