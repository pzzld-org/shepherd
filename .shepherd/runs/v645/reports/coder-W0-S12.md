## CODER REPORT
- Lane: l5-harness
- Skills loaded: code-style (SKILL.md; no `shell.md` file exists in code-style yet — applied shared principles per its own documented gap-handling instruction), shell (mastery skill: bash 3.2 portability, `set -e` if/while exemptions, mktemp/trap discipline, `$()`/`||`-guard idioms), and the project's own `skills/context/styles/shell.md` (the `[CODE-STYLE]` operator-preference doc this repo injects for `.sh` files — read directly since it is not a Skill-tool-registered skill)
- Files touched (created/modified/deleted): `/Users/jo3/src/fl03/shepherd/.worktrees/v645-l5-harness/hooks/tests/test_v644_wiring.sh` (modified)
- LOC delta: +155 / -38 (net +117; `git diff --numstat`)
- Acceptance grep results:
  - `bash hooks/tests/test_v644_wiring.sh` → **exit 1** (real FAIL — see Notes; this is the intended, correct outcome, not a defect in the test). Full output:
    ```
      PASS  #270 — git status --porcelain detects a real uncommitted change
      PASS  #270 — git diff --shortstat reports the real change size
      PASS  #269 — shepherd plan lane-drift v645 exits 0 (vars.json located, CLI wired)
      FAIL  #268 — shctx plan amend is documented in agents/shepherd.md but NOT wired: ERROR: unknown subcommand: amend
      PASS  #268 — the CLI implements amend (rejects on its own --plan validation, not on dispatch)
      PASS  layout — run-scoped audit/discovery write paths enforced (…/hooks/tests/test_lock_guard_write_path.sh)
      FAIL  v6.4.4 wiring — 1 missing leg(s)
    ```
  - `rg -c 'need .*"' hooks/tests/test_v644_wiring.sh | grep -qx 0` → **exit 1 as literally written** (see Deviations — ripgrep 15.2.0's `-c` prints nothing, not `0`, for a true single-file zero-match count, so `grep -qx 0` never sees a `0` line to match). Corroborated PASS with equivalent, version-independent checks: `rg -c 'need .*"' … ` → empty stdout, exit 1 (zero matching lines); `grep -c 'need .*"' …` → prints `0`; `grep -n 'need' hooks/tests/test_v644_wiring.sh` → **no output at all** — the substring `need` does not appear anywhere in the file (stronger than the required constraint).
  - `rg -q 'shepherd plan lane-drift .* ; test \$\? -eq 0' hooks/tests/test_v644_wiring.sh` → **exit 0 (PASS)**, matches line 101: `if ( shepherd plan lane-drift v645 ; test $? -eq 0 ) >/dev/null 2>&1; then`
- Halts encountered: none
- Summary: Replaced all 24 `need <file> "<string>"` prose-presence assertions in `test_v644_wiring.sh` with either a real invocation of the documented command/mechanism (git status/diff worktree-poll mechanics, `shepherd plan lane-drift v645`, `shctx plan amend` / `shepherd plan amend` reachability, delegation to the existing `test_lock_guard_write_path.sh` behavior gate) or an explicit `cite()` call under an `UNVERIFIABLE-IN-TEST` comment stating why the claim is a live-session/async platform behavior or an unenforced doctrine convention that a bash harness cannot trigger. `shellcheck` is clean (also removed a pre-existing, never-called `deny()` function that shellcheck flagged as dead code — confirmed via `git show <base-commit>:…` that it was unused before my change too). The rewrite genuinely surfaces a real, previously-invisible defect: `shctx plan amend`, the exact command `agents/shepherd.md` instructs root to run, errors `unknown subcommand: amend` today.
- Reporter: shepherd:coder (W0-S12) @ 2026-08-12T20:15:00Z

## Deviations
- **Acceptance command 2's literal exit code is 1, not 0, despite the file correctly satisfying its intent.** The command as specified — `rg -c 'need .*"' hooks/tests/test_v644_wiring.sh | grep -qx 0` — assumes `rg -c` prints a literal `0` line when a single-file search has zero matches. Empirically, ripgrep 15.2.0 (`rg --version` on this machine) prints **nothing** and exits 1 for that case (confirmed: `rg -c 'need .*"' hooks/tests/test_v644_wiring.sh` → empty stdout, `rg_exit=1`), so the downstream `grep -qx 0` receives no input and also reports no match. This is a ripgrep-version behavior quirk in the acceptance command itself, not a defect in the delivered file. Corroborating evidence the file is correct: (a) `grep -c 'need .*"' hooks/tests/test_v644_wiring.sh` (BSD/POSIX grep, which does emit `0` for zero matches) prints `0`; (b) an unqualified `grep -n 'need' hooks/tests/test_v644_wiring.sh` (no quote-adjacency requirement at all) finds **zero** occurrences of the substring `need` anywhere in the whole file — comments included — which is strictly stronger than what the acceptance criterion asks for. I did not alter the acceptance command; I ran it exactly as specified and report its true exit code, per the brief's "paste actual output/exit codes" instruction.
- **`bash hooks/tests/test_v644_wiring.sh` now legitimately exits 1** because the rewritten #268 leg for `shctx plan amend` genuinely fails: it is documented in `agents/shepherd.md` (`shctx plan amend --plan <plan.md> --reason "<what was wrong and how you know>"`, line 158) but the installed `shctx` CLI returns `ERROR: unknown subcommand: amend`. This is the correct, intended behavior of a real behavior test finding a real defect — see Notes for the full finding and its scope boundary.
- Removed a pre-existing, unused `deny(file, pattern, label)` function (defined at the original line 28, never called anywhere in the original file — verified via `git show 5be42280615c8dc5321061798240f476dffed645:hooks/tests/test_v644_wiring.sh | grep -n '[^_]deny '` → no output). `shellcheck` flagged it as SC2329 (dead code) once I started linting my rewrite; since it served no purpose before or after my change, I deleted it rather than carry dead code forward. This is a 2-line, in-scope, zero-behavior-change cleanup of the exact file I was already rewriting top to bottom.
- Section (D) (`#269b` plugin-root-relative helper-script paths, lines 146–162) and the `E1/E2/E5/E8/E9/E10` doc-convention checks in section (E) were left as text-based checks rather than converted to `need`-style-but-renamed-to-dodge-the-regex tricks. (D) is fundamentally a static-analysis check — the "behavior" under test IS the literal text of the doctrine files (does a citation carry `${CLAUDE_PLUGIN_ROOT}`?), so grepping it is not misrepresenting prose as runtime proof; it was never a `need`-based assertion and is untouched by the ACCEPTANCE regex. `E1/E2/E5/E8/E9/E10` are explicitly labeled `UNVERIFIABLE-IN-TEST` with a stated, specific reason each (see the file) rather than silently dropped, per `[NON-GOALS]`.

## Staged GH commands
None. No `gh` invocations were needed for this step — no issue/PR was opened, closed, or commented on. The one live defect this rewrite surfaces (`shctx plan amend` unwired — see Notes) is reported here for the conductor/root to act on; filing or updating a GitHub issue is outside a coder's authority (`CODER-GIT-WRITE`/git-custody boundary applies to `gh` writes too) and is not staged since I have no `gh issue create`/`gh issue comment` text to hand off — that decision (new issue vs. amend an existing one, e.g. #268's own follow-up) belongs to root/engineer with full sprint context I do not have.

## Notes — honest re-grade of #268/#269/#270 wiring status, for the CHANGELOG-vs-tracker divergence this sprint exists to fix

**#269 (lane-drift) — CONFIRMED WORKING, now proven by real invocation, not prose.**
`shepherd plan lane-drift v645` (the Python `shepherd_cli` entrypoint) exits 0 and reports `lane-drift: ok — 2 lane(s) agree (plan.md == vars.json)` against the live v645 run (`.shepherd/runs/v645/lanes/{l4-conformance,l5-harness}/vars.json`, resolved via the MAIN repo's `.shepherd/` namespace, not the worktree's own — `resolve_workdir()` follows `git rev-parse --show-toplevel`/`--git-common-dir` precedence, landing on the sprint root). This is real, current, and correctly documented. `agents/conductor.md`'s citation of `shepherd plan lane-drift` is accurate.

**#268 (amend) — PARTIALLY BROKEN. The doctrine names the wrong binary form, and a string check could never have caught it.**
`agents/shepherd.md:158` instructs root to run **`shctx plan amend --plan <plan.md> --reason "…"`** verbatim. I ran it for real (no mutating args): `shctx plan amend` → `ERROR: unknown subcommand: amend` (exit 1). I confirmed this is not a stale-installed-plugin artifact: this worktree's own `skills/context/scripts/cmd_plan.sh` (v6.4.5-in-progress) dispatches only `extract|topology|validate|hash|record-critique|verify` in its `case "$sub" in …` block — `amend` and `lane-drift` are simply never wired into the bash `shctx` CLI layer, even though `_USAGE` at the top of that same file's sibling, `services/cli/shepherd_cli/commands/plan.py:124`, already advertises `shctx plan <extract|topology|validate|hash|record-critique|verify|amend|lane-drift>`. The Python CLI (invoked as `shepherd`, not `shctx`) genuinely does implement `amend` — `shepherd plan amend` (no args) correctly fails its own arg validation (`ERROR: --plan <path> required and must exist`, exit 2), never "unknown subcommand." So: the underlying `_cmd_amend` logic is real and reachable, but the specific command root is told to type — the `shctx` form — is not wired to it. This is a live, reproducible, in-scope-to-report defect: root following `agents/shepherd.md`'s doctrine literally, today, cannot amend a plan. I did not touch `cmd_plan.sh` or `agents/shepherd.md` (both outside `[FILE-SCOPE]`) — flagging this for root/engineer to route to a fix (likely: wire `amend`+`lane-drift` into `cmd_plan.sh`'s dispatch case, or correct the doctrine to say `shepherd plan amend`).

**#270 (defensive poll) — the invocable HALF is proven real; the async half is honestly marked unverifiable, not silently dropped.**
The worktree-state-as-ground-truth fallback genuinely works: `git status --porcelain` and `git diff --shortstat` both correctly detect a real, deterministic scratch-repo change (proven in a throwaway `mktemp -d` repo, not the live sprint state). Whether the `Agent()` SDK's completion notification itself ever arrives, and how a `SendMessage` reply like `had no active task; resumed from transcript` should be interpreted, are live-session platform behaviors — genuinely outside what a bash test harness can trigger, exactly as the brief names. Those five citations (`#270` × 3 files, "treat it as absent, not as late", "had no active task") are now honestly labeled `UNVERIFIABLE-IN-TEST` rather than silently passing as if a grep hit proved the async behavior — this matches the brief's explicit example.

**Layout (run-scoped writes) — re-scoped, not re-implemented.** `hooks/tests/test_lock_guard_write_path.sh` already behavior-tests discovery/auditor run-scoped write-path enforcement end to end (builds its own scratch git repo + `.shepherd` namespace, feeds `lock_guard.sh` real PreToolUse JSON per role, asserts allow/deny — 11/11 passing when I ran it standalone). Rather than duplicate that role-resolution harness inline, `test_v644_wiring.sh` now invokes it directly and asserts its exit code — real delegated behavior evidence for the E3/E4/E6/E7 claims in one check. Separately, I found `agents/worker.md`'s parallel claim ("worker writes `{run_dir}/reports/`") has **no** hook-layer enforcement at all — `lock_guard.sh`'s own header explicitly excludes worker from its role-based constraint ("conductor / engineer / critic / worker / unknown — no write-path constraint at hook layer"). Unlike discovery/auditor, this is a pure, mechanically-unenforced doctrine convention; I labeled it `UNVERIFIABLE-IN-TEST` with that specific reason rather than lumping it in with the enforced pair.

**Divergence relevance:** the sprint's own framing — a string check reporting "fixed" while the underlying behavior silently regresses — is exactly what happened with `#268`'s `shctx plan amend`. The old test's `need agents/shepherd.md "shctx plan amend"` passed cleanly (the words are genuinely in the file) while the command they describe has been unreachable this entire time. This is not hypothetical: it is the live state of this branch, caught by the very rewrite this step asked for.
