## CODER REPORT
- Lane: W8-L6 (contradiction-ledger.md §2 finding #2 `shctx-alias-claim-vs-impl`, Wave 1 / L6)
- Skills loaded: code-style (markdown/prose editing — no dedicated language skill applies; this is a doc-only correction), shell (to verify `shctx:182-187` dispatcher behavior before rewording the doc)
- Files touched (created/modified/deleted): `/Users/jo3/src/fl03/shepherd/skills/context/SKILL.md` (modified — lines 13 and 15 only)
- LOC delta: +2 / -2 (markdown prose lines, not `.rs` — ONE-LOC rule production-Rust budget does not apply to this lane; no code, no tests, no build-manifest changes)
- Acceptance grep results:
  - `grep -n "alias shim\|alias/exec shim" skills/context/SKILL.md` → PASS (0 hits; the false "alias/exec shim" claim is gone, replaced by an explicit "is NOT an alias/exec shim to it" statement plus the accurate description)
  - `sed -n '180,192p' skills/context/scripts/shctx` → PASS (confirms cited `shctx:182-187` still matches: the `case "$cmd" in ... bash "$impl" "$@"` dispatch block that never invokes `bin/shepherd`)
  - Cross-check against `services/cli/shepherd_cli/__main__.py`, `resolution.py:find_bash_shctx`, and `services/cli/README.md:1-10` → PASS (confirms the *actual* relationship: `bin/shepherd` is the native Python/Typer CLI; for a small set of still-bash-only command groups it shells out TO `shctx` via `find_bash_shctx()` — the reverse of what the old doc claimed. The new doc text states this reverse-shim fact so the correction doesn't just delete a wrong claim, it replaces it with the true one.)
- Halts encountered: none
- Summary: Finding #2 (`shctx-alias-claim-vs-impl`) said `skills/context/SKILL.md:13` calls `shctx` an "alias shim" to `bin/shepherd`; `skills/context/scripts/shctx:182-187` is in fact a standalone bash `case`-dispatcher that routes each subcommand to a sibling `cmd_<subcommand>.sh` script and never calls `bin/shepherd` at all. Corrected line 13 to state the true relationship (both directions: `shctx` never calls `bin/shepherd`; `bin/shepherd` calls INTO `shctx` for a handful of un-ported groups via `find_bash_shctx()`) and touched the one downstream line (15, "always invoke the absolute path above") that had become ambiguous once line 13 named two paths instead of one — disambiguated it to "the `shctx` absolute path above" since line 15's own content (`command -v shctx`) is exclusively about `shctx`. Per the finding's resolution and the dispatch brief, did not touch `skills/context/scripts/shctx` itself (doc is the wrong side, not the code) and did not touch any sibling ledger file (`CHANGELOG.md:254`, the parallel finding #18 on the same root cause, is a different lane's file scope).
- Reporter: coder-W8-L6 @ 2026-08-13T00:00:00Z

## INSIGHTS
- kind: gap — `skills/context/SKILL.md:13`'s prior text only described one direction of the `bin/shepherd` ↔ `shctx` relationship, and described it backwards. The true relationship is bidirectional-but-asymmetric (native Python owns most groups; a handful of un-ported groups shim FROM Python INTO bash), which is easy to get backwards again in a future edit. A future doc pass could add one line to `services/cli/README.md` or `SKILL.md` making this asymmetry a single canonical, cross-referenced sentence instead of restating it per-file.
