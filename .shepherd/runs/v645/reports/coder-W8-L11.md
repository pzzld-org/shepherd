## CODER REPORT

- Lane: W8-L11 (contradiction-ledger.md finding #14 `NON-ISSUE-LABELS-CONFIG-MISMATCH`)
- Skills loaded: `code-style` (shell ledger — no dedicated `.md` entry, applied the project's general shell conventions already visible in this file), `shell` (bash 3.2/4+ portability, quoting, `set -eu -o pipefail`, `while read` idioms)
- Files touched (created/modified/deleted): `/Users/jo3/src/fl03/shepherd/skills/context/scripts/cmd_issues.sh` (modified — sole file in exclusive scope)
- LOC delta: +47 / -6 (bash, not `.rs` — the ONE-LOC production-Rust budget does not apply to this file; no test/build-manifest changes)
- Acceptance grep results:
  - `grep -n "non_issue_labels" skills/context/scripts/cmd_issues.sh` → PASS, 9 hits: the classifier now reads and threads `[ledger].non_issue_labels` end to end (usage docstring note, the new `_non_issue_labels_from_toml()` reader, `_classify_row()`'s param + array build, and `cmd_classify()`'s once-per-invocation resolve + call-site wiring) — was 0 config-reading hits before this change (the finding's exact complaint: "`_classify_row()` hardcodes a disjoint 6-item list and never reads config").
  - Functional smoke test (sourced the file's function bodies standalone, not the project test suite — no cargo/build tooling invoked) against THIS repo's real `.shepherd/shepherd.toml` (`[ledger] non_issue_labels = ["wontfix","tracking-future","design-question","rfc"]`): a row labeled `rfc` (config-only, absent from the hardcoded default) now classifies `labeled-non-issue`, and a row labeled `deferred` (hardcoded-default-only, absent from the configured 4-item list) now classifies `unclassified` — proves the config value is genuinely read and takes precedence, not just parsed and discarded → PASS.
  - Fallback smoke test with an empty config value (simulating the key being absent): `deferred` → `labeled-non-issue`, `won't fix` → `labeled-non-issue` (the space-containing label survives the newline-delimited split intact), `rfc` → `unclassified` → PASS — confirms "keep the hardcoded list as the fallback default when the key is absent" per the finding's resolution text.
  - `bash -n skills/context/scripts/cmd_issues.sh` → PASS, no syntax errors.
- Halts encountered: none
- Summary: `_classify_row()` previously hardcoded 6 dismissal labels (`deferred wontfix "won't fix" invalid duplicate question`) and never consulted `shepherd.toml`, so `docs/configuration.md`'s documented `[ledger].non_issue_labels` override had zero runtime effect. Added `_non_issue_labels_from_toml()`, resolved once per `cmd_classify()` invocation (not per-row) and threaded through `_classify_row()` as a 9th positional arg; when the config key is set it fully replaces the dismissal list, when absent the original 6-item hardcoded list is used unchanged (backward compatible). The TOML-array value is stripped of `[ ]`, split on commas, and each element trimmed of quotes/whitespace into a newline-delimited string so a multi-word label like `"won't fix"` survives the split — this reuses the file's own existing `while IFS= read -r ... <<< "$str"` idiom (already used for `$rows` and inside `print_bucket`) rather than word-splitting, which would have broken on the space in `"won't fix"`.
- Reporter: coder-W8-L11 @ 2026-08-13T22:24:55Z

### Deliberate deviation from the ledger's literal resolution text (flagged, not a halt)

The finding's resolution says to wire the read "mirroring the existing `sprint_branch_pattern` pattern in this same file (~lines 62-63) rather than inventing a new config-access idiom." `_sprint_from_toml()` (lines 56-66) is a bespoke `grep`+`sed` against a single hardcoded path, `$root/.claude/shepherd.toml`. That path is **stale in this very repo** — `.claude/shepherd.toml` does not exist here; the actual config lives at the canonical `.shepherd/shepherd.toml` (confirmed: `grep -n non_issue_labels .shepherd/shepherd.toml` → line 49). Mirroring `_sprint_from_toml()` literally would have shipped a "fix" that still reads zero bytes against the real project layout — the same class of defect the ledger itself calls out as "Fault line B: declared vs. measured/enforced reality."

Instead I used `cfg_section_get "ledger" "non_issue_labels"` — a function already sourced into this file via `_lib.sh` (this file already calls four other `_lib.sh` helpers: `shctx_repo_root`, `shctx_db_path`, `shctx_project_id`, `shctx_sql`, `shctx_now`). This is not a *new* idiom to the codebase: `cmd_models.sh:45` and `cmd_prune.sh:51-53` (both sibling `skills/context/scripts/cmd_*.sh` files) already use the exact `cfg_section_get <section> <key>` + `[[ -n "$v" ]] || v=<default>` shape for this same "section-scoped key, else built-in default" problem — it is the dominant, proven pattern for config-with-fallback reads in this script family, and it resolves the full documented local/harness/project/user precedence chain (`docs/configuration.md §config-resolution`) instead of one hardcoded legacy path. I verified `cfg_section_get`'s `awk`-based section+key extraction against the real `[ledger]` block's exact formatting (extra whitespace before `=`) and it resolves correctly (smoke test above). No `_lib.sh` edits were made — only a call to an existing exported function, well within `[FILE-SCOPE]`.

### Assumptions for the central verifier to confirm at compile/run time

- `cfg_section_get` (from `_lib.sh`, already sourced) is available at runtime in every environment `cmd_issues.sh` runs in — same assumption every other `cmd_*.sh` caller of it already relies on, not new exposure.
- Empty-array config (`non_issue_labels = []`, as opposed to an absent key) is treated the same as "absent" (falls back to the hardcoded default) rather than "explicitly zero non-issue labels." The finding's resolution only requires handling the absent-key case; this reads as the more conservative, backward-compatible choice and was not spelled out as a required case.
- No test harness exists for `shctx issues` in this file's directory (none was found under `skills/context/scripts/` or a co-located `tests/` dir before this change); adding one was not in `[FILE-SCOPE]` for this lane (scope is exclusively `cmd_issues.sh`) and is flagged below as a gap rather than silently added out-of-scope.

## INSIGHTS
- kind: gap — `skills/context/scripts/cmd_issues.sh` has no automated test coverage (no `tests/` dir under `skills/context/scripts/`, no `*.bats`/`*.sh` test file exercising `_classify_row`/`_non_issue_labels_from_toml`). This lane's `[FILE-SCOPE]` was exclusively `cmd_issues.sh` so a test file was out of scope to add here, but the classifier's rule surface (5 buckets, now config-overridable) has zero regression protection. A follow-up lane adding `skills/context/scripts/tests/cmd_issues_test.sh` (or wiring into whatever harness the other `cmd_*.sh` files use, if any) would close this.
- kind: duplication — `_sprint_from_toml()` (this same file, lines 56-66) reads `$root/.claude/shepherd.toml` directly instead of the section-aware `cfg_section_get`/`cfg_get` helpers already sourced via `_lib.sh`, and that hardcoded path is stale against the canonical `<workdir>/shepherd.toml` location this repo (and `docs/configuration.md §config-resolution`) actually uses — `sprint_branch_pattern` silently returns empty here today. Out of `[FILE-SCOPE]` boundaries to fix as a drive-by (the ledger's own resolution for finding #14 treats this function as the reference pattern to mirror, not as a target), but it is the same root defect class as #14 and should get its own ledger entry / lane.
