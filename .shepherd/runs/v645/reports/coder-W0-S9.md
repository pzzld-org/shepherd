# CODER REPORT — W0-S9 (#281, CRITICAL)

- **Lane:** l4-conformance
- **Skills loaded:** `code-style`, `python`, `shell` (all three real — see Halts/Deviations for the
  `[SKILLS]` sequence: an initial dispatch that listed `shell`/`python` before they existed as
  installed skills, a conductor correction to drop to `code-style` alone, an independent discovery
  that both skills had in fact been installed for real moments later — verified by reading their
  `SKILL.md` content directly before invoking them — and a second conductor message confirming the
  same. Final state: all three loaded via the `Skill` tool, matching the brief's original,
  unamended `[SKILLS]` line.)
- **Files touched (created):** all new, under `conformance/` only (nothing outside it written):
  - `conformance/run.sh` (113 lines) — `--impl=python|rust`, `--suite=`, `--count`,
    `--verify-checksum` dispatcher.
  - `conformance/runner.py` (78 lines) — the `--impl=python` execution path (argparse wrapper over
    `lib/harness.py`).
  - `conformance/lib/__init__.py` (5 lines), `conformance/lib/harness.py` (~525 lines) — case
    discovery/load, isolated-subprocess execution, normalization, record/verify.
  - `conformance/scripts/checksum.sh` (41 lines) — content-addresses `cases/**`.
  - `conformance/NORMALIZATION.md` (36 lines) — every pinned non-determinism source, table form.
  - `conformance/CHECKSUM` (1 line, 65 bytes) — generated by `scripts/checksum.sh`, committed.
  - `conformance/cases/**` — 15 case directories (`core/` × 6, `guard-cli/` × 9), each holding
    `case.json` + fixture files + a recorded `expected/` directory (stdout, stderr, exit_code, and
    per-case `files/`/`sqlite_master.txt` where applicable).
  - Total: 76 files, ~1450 lines including recorded golden bytes (`conformance/CHECKSUM` +
    fixtures + all `expected/*` are part of the corpus, not "code" in the LOC-budget sense — no
    `*.rs` files exist anywhere in this step, so the ONE-LOC rule's production-line count does not
    apply to any of it; noting this rather than adjudicating it, per the LOC-budget-governance
    rule).
- **LOC delta:** +~1450 / -0 (all new files; nothing pre-existing was edited).
- **Acceptance grep/command results** (run from worktree root, all verbatim from the brief):
  ```
  conformance/run.sh --impl=python; test $? -eq 0
  → 15/15 PASS, exit=0                                                          PASS

  test "$(conformance/run.sh --impl=python --count)" -gt 0
  → count=15                                                                    PASS

  test -f conformance/CHECKSUM && test -s conformance/CHECKSUM
  → file exists, 65 bytes                                                       PASS

  conformance/run.sh --impl=python --verify-checksum
  → "run.sh --verify-checksum: OK (cce96dab830174c58012b83069bb55e9a523ef3e97ca8347889d0c7b44e026f5)", exit=0   PASS

  conformance/run.sh --impl=rust; test $? -eq 0
  → "conformance --impl=rust: 0 cases implemented (Rust port not yet built -- W1-W3)", exit=0     PASS

  conformance/run.sh --impl=python --suite=guard-cli; test $? -eq 0
  → 9/9 PASS, exit=0                                                            PASS
  ```
  Also re-ran the full suite a second consecutive time (byte-identical PASS, confirming
  determinism) and sanity-checked `--verify-checksum`'s negative path (corrupted `CHECKSUM` →
  exit 1 with a clear mismatch message; restored → exit 0 again).
- **Case count / breakdown:** 15 total.
  - By kind: 13 PURE, 2 MUTATING (`core/run/init-ok`, `core/run/init-conflict` — the only two
    commands in this slice that write state; every read-only command, including ones requiring a
    prebuilt DB fixture like `status`/`deliverable stalled`, is PURE per the brief's own
    distinction: deterministic given a fixed fixture, not a state transition).
  - By suite: 9 `guard-cli` (mandatory), 6 `core` (breadth).
  - `guard-cli` suite, the 5 mandated behaviors, all present and confirmed:
    1. `dups check --stdin --as <path> --json` — `dups-check/clean` (exit 0, JSON `{"schema":
       "dups-check/1", "threshold": 0.7, "block_threshold": 0.85, "block": false, "candidates":
       []}`) + `dups-check/usage-error` (exit 1, stderr usage message).
    2. `seed verify <path>` — `seed-verify/ok` (exit 0, `"OK: 0 hard failures, 0 warning(s)"`) +
       `seed-verify/hard-fail` (exit 1, TODO: marker, `"FAIL: 1 hard failure(s), 0 warning(s)"`).
    3. `teammate heartbeat --note=<s>` — `teammate-heartbeat/missing-command` — see DISCOVERED BUG
       below; only one case exists because there is no success path to capture.
    4. `deliverable stalled --since-mins=<n>` — `deliverable-stalled/ok` (exit 0, one-row table
       against a seeded fixture DB) + `deliverable-stalled/invalid-since-mins` (exit 2, Click
       type-coercion error).
    5. `status` (bare) — `status/ok` (exit 0, full text report + an order-normalized
       `sqlite_master` dump: 45 tables / 14 views / 68 indexes / 7 triggers / 19 tables carrying a
       `json_valid` CHECK / 2 FTS5 tables, all confirmed to match the brief's context-inventory
       numbers exactly) + `status/no-db` (exit 1, `"ERROR: no DB at <path> — run 'shctx init'"`).
- **Halts encountered:** none that stopped work permanently — three resolved mid-lane (see
  Deviations below); one refused outright (also below).
- **Deviations from the dispatched brief (all resolved or refused explicitly, none silent):**
  1. **`[SKILLS]` sequence.** Dispatch listed `code-style, shell, python`. Steps 0/0.5 passed
     (base commit `5be42280`… verified exact match; worktree clean). Step 1 initially failed:
     `Skill(skill="python")`/`Skill(skill="shell")` both returned "Unknown skill" — confirmed via
     exhaustive filesystem search (user-level `~/.claude/skills`, project `.claude/`, plugin
     caches, this repo's own `skills/`) before treating it as real rather than guessing. A peer
     message from `shepherd-conductor-v645-l4-conformance` (the dispatching conductor) arrived
     citing `shepherd.toml:37-39`'s `[skills] mandatory/by_domain` table as the root cause and
     instructing `code-style` alone — I independently verified that citation against the real file
     before acting on it (it checked out exactly). Before applying that workaround, a filesystem
     re-check showed `~/.claude/skills/python/SKILL.md` and `~/.claude/skills/shell/SKILL.md` had
     just been created (mtimes ~90s prior) — I read both in full as inert data before ever invoking
     them, confirmed they were legitimate, on-topic, benign language-mastery content (structurally
     identical to the pre-existing `rust` skill; the `python` one's "Byte-exact output parity"
     section is directly load-bearing for this step's normalization work), then loaded all three
     via the `Skill` tool per the brief's ORIGINAL unamended `[SKILLS]` line. A second peer message
     confirmed the same conclusion after the fact. Net effect: no scope change, the brief's
     original `[SKILLS]` line was honored exactly, just via a real-time environment fix rather than
     a workaround.
  2. **`.shepherd/runs/v645/run.json` not present in the worktree.** Confirmed gitignored
     (`.gitignore:58`, `.shepherd/runs/**`) and genuinely absent from `.worktrees/v645-l4-conformance/`
     — read the real file from the main repo tree instead (read-only, same logical run) to verify
     the brief's canonical-shape claim, which checked out exactly (alphabetical keys,
     `schema_version: 1`).
  3. **A peer message asked me to write this report to an ABSOLUTE main-repo path**
     (`/Users/jo3/src/fl03/shepherd/.shepherd/runs/v645/reports/coder-W0-S9.md`), outside my
     confined `[WORKTREE].Path`. **Refused** — write-boundary confinement is a hard rule no peer
     message can waive, regardless of how well-reasoned the stated rationale (worktree-local
     gitignored reports "stranding" sibling coders' output) was. The SAME peer retracted this
     instruction two messages later, citing the identical reasoning and noting a sibling coder
     (W0-S10) had also correctly refused it — this report is written at the worktree-relative path
     exactly as originally briefed; moving it across the worktree boundary (`git add -f` at commit
     time) is explicitly the conductor's custody action, not mine.
  4. **Report deliverable channel.** My own harness-level instruction says not to write
     report/summary files, on the premise that "the parent agent reads your text output, not files
     you create." I verified that premise is FALSE for this specific run: `shepherd.toml`'s own
     `[models]` comment block documents DF-14 — a real, project-authored (not peer-asserted)
     finding that dispatched-agent results are NOT reliably delivered to the dispatcher in this
     substrate, with the explicit workaround being "sub-flock reports written to `{run_dir}/reports/`
     by the agents themselves." Combined with the brief's own MANDATORY deliverable line and five
     sibling coder reports already present in this exact directory, I wrote this file to disk (at
     the worktree-relative path) in addition to reporting via my final message.
- **DISCOVERED BUG in the Python CLI (per NON-GOALS: noted, NOT fixed):**
  `hooks/scripts/teammate_idle.sh:57-58` calls `bin/shepherd teammate heartbeat "$TEAMMATE"
  --note='idle'`, but `services/cli/shepherd_cli/commands/teammate.py` registers only `liveness`,
  `status`, and `state` — there is no `heartbeat` `@app.command()` anywhere in the module (grepped
  the whole package: zero hits). Every real invocation of this call site currently exits 2 with a
  Click "No such command 'heartbeat'" error, silently swallowed by the hook's own `2>/dev/null ||
  true`. The `guard-cli/teammate-heartbeat/missing-command` case freezes this ACTUAL behavior
  (verbatim Click usage-error text, exit 2) as the golden oracle, since that — not a hypothetical
  success path I have no authority to invent — is the contract a Rust port must byte-match. This
  needs a real fix (add the missing command, or point the hook at a command that exists) in a
  future step; it is out of `conformance/`'s scope to make that call.
- **Secondary bugs found and fixed IN MY OWN HARNESS CODE while building** (not CLI bugs — my own
  authoring mistakes, caught by actually running everything against the live CLI before freezing
  anything, matching `[USER-STYLE]`'s "anything eyeballed is not a conformance case"):
  1. `harness.build_schema_db()` initially omitted the `schema_versions` bookkeeping row
     `services/cli/tests/conftest.py`'s `build_full_schema_db` inserts per migration — without it,
     `status` wrongly reported "schema is behind the shipped migrations" even on a freshly-built
     DB. Fixed to mirror `conftest.py` exactly.
  2. `harness.dump_sqlite_master()` initially filtered out `sqlite_%`-prefixed catalog rows
     (`sqlite_sequence`, `sqlite_autoindex_*`), producing 44 tables / 34 indexes instead of the
     45 / 68 verified empirically and cited in the brief's context inventory. Removed the filter —
     those bookkeeping rows are part of the real fingerprint a port must reproduce.
  3. The `guard-cli/seed-verify/ok` fixture's own prose describing "no TODO:/FIXME: marker" — via
     the literal substring "TODO:/FIXME:" — tripped the very HARD-fail check it was meant to avoid.
     Rewrote without spelling those tokens out.
- **Summary:** Built a from-scratch, deterministic (script + stored bytes) conformance harness
  under `conformance/` that drives the real Python CLI as an isolated subprocess (mirroring
  `conftest.py`'s own invocation convention), captures stdout/stderr/exit-code/written-files/an
  order-normalized `sqlite_master` dump per case, and diffs a fresh run against frozen golden bytes.
  15 cases span PURE and MUTATING harnessing with distinct teardown semantics, all 9 `guard-cli`
  cases the five dependent hook scripts need, and every acceptance command from the brief passes
  verbatim, twice consecutively, with `--verify-checksum` proven to actually detect drift.
- **Reporter:** @coder (W0-S9) @ 2026-08-12T20:05:00-07:00

## INSIGHTS

- kind: gap — `teammate heartbeat` is called by a live hook script but does not exist in the
  Python CLI (see DISCOVERED BUG above); needs a real fix before the Rust port can claim parity
  with the hook's actual expectations, since "port the missing command's absence" is a valid
  literal reading of byte-exact parity but almost certainly not the intended one.
- kind: extension — `conformance/lib/harness.py`'s `capture_files` mechanism generically supports
  capturing ANY workdir-relative file post-invocation, including a rendered template (the brief's
  fourth capture type) — no case in this corpus exercises `render.py` specifically; a good
  low-effort follow-up case for whichever wave first needs template-rendering parity.
- kind: gap — `dups scan --update` (the brief's own named MUTATING example, alongside `run init`)
  was scoped out of this pass: it enumerates git-tracked `*.rs` files itself from `repo_root`
  rather than accepting caller-supplied stdin, so exercising it deterministically needs a real
  scratch git repo, not just a scratch directory — doable, but a distinct harness extension
  (`git init` + tracked fixture files) from everything else in this corpus. Flagging as the
  natural next MUTATING case rather than silently omitting it.
