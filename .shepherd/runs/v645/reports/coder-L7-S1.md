## CODER REPORT
- Lane: L7-S1
- Skills loaded: code-style (no markdown-specific language skill exists in the installed set; `commands/spawn.md` is prose/doctrine, not source code — code-style's general discipline (no TODO/FIXME, SUBTRACT over append, precise citation) applied throughout)
- Files touched (created/modified/deleted): /Users/jo3/src/fl03/shepherd/commands/spawn.md (modified)
- LOC delta: +51 / -13 (markdown doc file; not part of any *.rs LOC budget)
- Acceptance grep results:
  - `grep -n "teammateMode" commands/spawn.md` → 5 matches (Preflight table row 1 + Check 1 steps 1/2/2-example/5) — PASS
  - `grep -n "claude-swarm-<lead-pid>" commands/spawn.md` → 2 matches (Check 1 step 4, both the oracle command and the dedicated-socket explanation) — PASS
  - `grep -n "backendType" commands/spawn.md` → 5 matches (table row 1, existing team-lead JSON example, DF-66 narrative, `[SUBSTRATE]` print line, Check 1 step 4 oracle) — PASS
  - explicit UNTESTED/not-established language for `auto` → Check 1 step 5: "`auto` resolution is UNTESTED and box-dependent — do not assert an outcome. What `teammateMode: "auto"` resolves to on any particular machine is not established here" — PASS
  - permission-mode-inheritance point present → Check 1 step 6, text unchanged from original (renumbered 3→6) — PASS
  - downgrade-to-`/shepherd:start` closing line present → unchanged closing paragraph after the numbered list — PASS
  - `grep -c "^### Check 1" commands/spawn.md` → 1 — PASS (edited in place, not duplicated)
- Halts encountered: none
- Summary: Rewrote Preflight table row 1 and the "### Check 1 — substrate verification" subsection in `commands/spawn.md` per DF-66/DF-68. Check 1 now: (1) keeps the env-var check as necessary-not-sufficient with the original probe-note detail folded in; (2) adds a resolution step that reads `teammateMode` from `~/.claude/settings.json` and, for `auto`, resolves it via `$TMUX` OR (iTerm2 + `it2` on PATH) else in-process, printing `[SUBSTRATE] teammateMode=<mode> → predicted backendType=<tmux|in-process>` before the spawn instruction fires; (3) states plainly what tmux (separate CLI process, MAIN session, keeps Workflow+ScheduleWakeup, no ungranted write tools) and in-process (subagent, loses Workflow+ScheduleWakeup even when granted, has been OBSERVED GAINING ungranted Edit/Write/Artifact on read-only `shepherd:conductor` — flagged explicitly as a CONTAINMENT failure per DF-65) each carry; (4) names the correct post-spawn oracles — `backendType`/`tmuxPaneId` in `~/.claude/teams/<team>/config.json` and `tmux -L claude-swarm-<lead-pid> ls` — and explains why bare `tmux ls` is a structural false negative (DF-68); (5) states `auto`'s resolution is UNTESTED/box-dependent and must never be inferred from whether the lead is itself already inside tmux, since Claude Code spawns its own private-socket tmux server (DF-66 self-correction); (6) keeps the permission-mode-inheritance point and the "failed verification → DOWNGRADE to /shepherd:start" closing line unchanged. No new HARD-gating logic was added — the only refusal branch remains the pre-existing unset-env-var → downgrade path; the backend resolution is purely VERIFY/advisory (print + inform). Did not touch `skills/harness/SKILL.md` or `skills/shepherd/SKILL.md` (sibling steps' scope — both show as modified in `git status` from concurrent siblings, not from this edit; confirmed via `git diff -- commands/spawn.md` showing only this file's hunk).
- Reporter: coder-L7-S1 @ 2026-08-13T22:00:28Z

## INSIGHTS
- kind: nit — Check 1's original numbered list mixed the "substrate absent entirely" branch (env-var check + probe note) with the "substrate present, which backend" branch (was previously entirely missing) as siblings; the rewrite makes the two-phase structure (env presence, then backend resolution) explicit via ordering, which should make future dogfood findings about either phase easier to locate and amend without touching the other.
