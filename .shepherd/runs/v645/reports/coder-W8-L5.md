## CODER REPORT
- Lane: W8-L5
- Skills loaded: code-style (general discipline — no TODO/FIXME, precise citation, SUBTRACT-aware edits), shell (bash 3.2.57 idioms for `conformance/run.sh` — `[[ ]]`, `set -euo pipefail`-safe explicit `exit`, stderr-for-failure convention already established elsewhere in the file). No `[SKILLS]` header was present in the dispatch (this brief is a pointer into the contradiction-ledger synthesis report, not a per-lane brief template) — these are the mandatory minimums for the two touched file types (`.sh`, `.md` doctrine prose).
- Files touched (created/modified/deleted):
  - `/Users/jo3/src/fl03/shepherd/conformance/run.sh` (modified)
  - `/Users/jo3/src/fl03/shepherd/skills/shepherd/references/pipeline.md` (modified)
- LOC delta: `conformance/run.sh` +19/-8 (net +11, shell — not `.rs`, does not count toward any Rust LOC budget); `pipeline.md` +1/-1 (net 0, one prose sentence-run replaced in place, markdown — not `.rs`, does not count).
- Acceptance grep results:
  - **Before/after exit codes, read directly from `$?` (never piped)** — the exact proof the brief demanded:
    - `conformance/run.sh --impl=rust` — BEFORE: `0`. AFTER: `1`. — PASS (now falsifiable)
    - `conformance/run.sh --impl=rust --suite=render` — BEFORE: `0`. AFTER: `1`. — PASS
    - `conformance/run.sh --impl=rust --count` — BEFORE: `0`. AFTER: `0` (unchanged — `--count` is informational, reports a number not a verdict, per usage doc lines 24-25 and `plan.md:1079`'s own `test "$(... --count)" -gt 0` usage pattern) — PASS (intentionally unaffected)
  - `grep -n "Falsifiability is mandatory" skills/shepherd/references/pipeline.md` → 1 hit, inside `§Gates`'s "Outcome enforcement" paragraph (line 117) — PASS
  - `grep -c "exit 1" conformance/run.sh` → 6 (new fail-closed `exit 1`, its two `printf ... >&2` guards, plus incidental doc-comment mentions of "exit 1") — PASS (was 0 occurrences of a reachable `exit 1` in the `--impl=rust` branch before)
  - `grep -n "#10\|DF-59" conformance/run.sh` → 2 hits (top-of-file usage comment + inline branch comment), both citing the finding this fix resolves — PASS
  - `bash -n conformance/run.sh` → exits 0, no syntax errors — PASS
  - `git status --short -- conformance/run.sh skills/shepherd/references/pipeline.md` → only these two files modified, confirming `[FILE-SCOPE]` was not exceeded — PASS
- Halts encountered: none
- Summary: Finding #10 (`ACCEPTANCE-PREDICATE-RERUN-VS-UNFALSIFIABLE`, DF-59) — `agents/auditor.md:98` mandates every acceptance predicate be re-run at CLOSE with a now-false result capping the grade, but `conformance/run.sh`'s `--impl=rust` run-mode branch was hard-coded to `exit 0` unconditionally (any `--suite`, including a nonexistent one), so a predicate written against it could never observe a failure. Fixed both sides: (1) `conformance/run.sh` lines ~90-107 (was 90-101) now fail CLOSED — `--impl=rust` in run mode (not `--count`) prints a `FAIL` message to stderr and exits `1` whenever 0 cases are implemented, which today is always, since no Rust conformance cases exist yet; `--count` is left untouched (exit 0, prints `0`) because it is explicitly documented as introspection, not a verdict, and `plan.md:1079` already relies on that exit-0-regardless-of-count contract for the python side. The stale top-of-file usage comment and the inline branch comment were both rewritten to describe the new fail-closed contract instead of the old "always exits 0, that's the point" rationale. (2) `skills/shepherd/references/pipeline.md §Gates`, inside the existing "Outcome enforcement" paragraph, gained a new "Falsifiability is mandatory (#10)" clause defining an unfalsifiable predicate (a branch hard-coded to succeed regardless of what ran) as disqualifying — not a gate — and assigning enforcement to `@critic` at Seam 2 (`PLAN-MISSING-OUTCOME-VERIFICATION`) and to wave-review REDO for predicates a coder introduces mid-wave. Did not touch `plan.md`'s stale `W0-GATE`/`W1-GATE` command examples (out of `[FILE-SCOPE]`; those are a separate doctrine-drift surface already flagged in `dogfood.md` DF-41/DF-59 and tracked to other lanes) nor implement `--assert-reproducible` or author `conformance/cases/render/` (DF-59's other two action items — explicitly out of this lane's two-file scope per the dispatch brief). No cargo/build/test command was run; only the shell script itself was invoked directly (no compilation, no venv, no Rust toolchain touched), consistent with resource discipline #256. Assumption for the central verifier: none requiring compile-time confirmation — both changes are prose/shell only, already exercised directly above with verbatim exit codes.
- Reporter: coder-W8-L5 @ 2026-08-13T22:19:20Z
