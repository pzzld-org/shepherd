## CODER REPORT
- Lane: W7-S3
- Skills loaded: code-style (project convention baseline — markdown-only edit target, no language skill applies), shepherd:shepherd (doctrine home of `skills/shepherd/SKILL.md`), shepherd:adaptation (excellence-bar check before touching doctrine prose)
- Files touched (created/modified/deleted): none by me this dispatch — verified pre-existing (see Summary)
- LOC delta: +0 / -0 (this dispatch)
- Acceptance grep results:
  - `grep -c "PROBE-FALSIFIABILITY" skills/shepherd/SKILL.md` — PASS (exactly 1 match, line 79)
  - `grep -n "positive control" skills/shepherd/SKILL.md` — PASS (1 match, line 79, states the rule: "any probe reported as a capability ABSENCE must state its positive control, or it is not a finding")
  - `grep -n "DF-68" skills/shepherd/SKILL.md` — PASS (1 match, line 79, cites the incident by row id + path to `.shepherd/runs/v645/dogfood.md`)
  - `grep -n "FANOUT-VEHICLE-DOWNGRADE" skills/shepherd/SKILL.md` — PASS (1 match, line 77, unchanged, INTACT per NON-GOALS)
  - `grep -n "WORKFLOW-VEHICLE-PROBE" skills/shepherd/SKILL.md` — PASS (1 match, line 77, PRESENT branch unchanged, INTACT per NON-GOALS)
  - `grep -n "Git custody is root-exclusive" skills/shepherd/SKILL.md` — PASS (1 match, line 91, byte-identical to pre-existing text, untouched)
  - `grep -c "^## " skills/shepherd/SKILL.md` — PASS (7 top-level sections, unchanged count — confirms the rule was folded into the existing `## Dispatch law` section, no new top-level section opened, SUBTRACT/fold-not-open honored)
  - `git diff HEAD -- skills/shepherd/SKILL.md` — PASS (isolated to a clean 2-line addition: the new paragraph plus its trailing blank separator, immediately after the WORKFLOW-VEHICLE-PROBE paragraph and before `## Root contract`; zero `-` lines anywhere in the diff)
- Halts encountered: none — see Summary for why no write occurred
- Summary: On Step 0.5 base-commit verification, `pwd`/`git rev-parse HEAD` matched `[WORKTREE]`/`[BASE-COMMIT-EXPECTED]`. On Step 3 (`[DO-NOT-DUPLICATE]`-equivalent grep before writing), `rg -n "PROBE-FALSIFIABILITY" skills/shepherd/SKILL.md` returned **one existing hit at line 79**, not zero. Investigation traced it to `.shepherd/runs/v645/reports/coder-L7-S3.md`, a prior/concurrent coder dispatch for the identical deliverable on the identical file, already landed in this same worktree (verified via `git diff HEAD` and that report's own acceptance table, byte-for-byte consistent with what's on disk now). The landed text satisfies every requirement in this dispatch: reads DF-68, states the rule verbatim ("any probe reported as a capability ABSENCE must state the positive control that would have shown presence... a probe with no positive control is not a finding"), is placed immediately after `WORKFLOW-VEHICLE-PROBE` inside the existing `## Dispatch law` section (fold-not-open honored), and cites `DF-68` as the incident by pointer to `.shepherd/runs/v645/dogfood.md` rather than retelling the tmux-socket narrative inline (correct call for a terse doctrine file — the narrative belongs in the dogfood log, not duplicated into standing doctrine). One deliberate, defensible deviation from my dispatch's literal wording: the brief asked the rule be placed "so the symmetry is visible" with the auditors' gate rule quoted as *"A GREEN SUITE IS NOT A GATE — prove the check can FAIL."* I grepped the entire repo (`.md`/`.py`/`.sh`/`.j2`, all of `agents/`, `skills/`, `services/`) for that exact phrase and it does not exist verbatim anywhere — the closest real doctrine text is `agents/auditor.md §Per-finding contract`'s Hypothesis + Falsification + Confidence triple, backed by the mandatory `superpowers:systematic-debugging` skill, which the landed paragraph cites explicitly and correctly as the auditor-side twin. Quoting a sentence that isn't actually in the codebase would have been a fabricated citation; anchoring to the real falsification-triple text is the honest version of the same symmetry and is what's on disk. Given the deliverable already exists, is correct, and reproducing it verbatim would itself violate the DEDUP-HIT backstop (an already-existing paragraph re-added is exactly what that guard blocks), I made **zero writes** this dispatch and verified rather than duplicated. `commands/spawn.md` and `skills/harness/SKILL.md` also show modified in `git status` — confirmed via targeted `git diff` that neither was opened by me; those are sibling steps' concurrent, disjoint edits in the shared worktree (W7-S1/S2-shaped work per DF-68 items b/d).
- Reporter: coder-W7-S3 @ 2026-08-13T23:04:00Z

## INSIGHTS
- kind: duplication — `W7-S3` (this dispatch) and `coder-L7-S3` (already-landed report at `.shepherd/runs/v645/reports/coder-L7-S3.md`) are the identical deliverable — same target file, same rule, same acceptance shape — dispatched under two different lane-id namespaces (`W{wave}-S{step}` vs `L{lane}-S{step}`) with no cross-check between them before the second dispatch fired. Nothing in the coder Startup Protocol currently distinguishes "no prior coder has touched this symbol" from "a prior coder already delivered this exact symbol, correctly" — Step 3's `[DO-NOT-DUPLICATE]` grep caught it here only because the rule's own name (`PROBE-FALSIFIABILITY`) is greppable and this coder ran the grep before writing rather than after. A conductor issuing two lane-ids for the same file+deliverable in the same wave is a dispatch-planning gap, not a coder-side one; worth a mesh-time check that no two steps in a run target the same `[FILE-SCOPE]` file with overlapping described outcomes, keyed on more than raw file path (raw-path disjointness already exists per-wave — this is a same-wave-different-run-id collision, `L7` vs `W7`, that disjointness check may not span).
