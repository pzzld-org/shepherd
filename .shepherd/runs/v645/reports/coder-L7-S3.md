## CODER REPORT
- Lane: L7-S3
- Skills loaded: code-style (project convention baseline — markdown-only edit, no language skill applies), shepherd:shepherd (doctrine home of the edited file), shepherd:adaptation (excellence-bar check before writing doctrine prose)
- Files touched (created/modified/deleted): /Users/jo3/src/fl03/shepherd/skills/shepherd/SKILL.md (modified)
- LOC delta: +2 / -0 (one new paragraph line + one blank separator line; this is a `.md` doctrine file, not `*.rs`, so the ONE-LOC production-Rust budget rule does not apply — noted for completeness, not adjudicated)
- Acceptance grep results:
  - `grep -n "PROBE-FALSIFIABILITY" skills/shepherd/SKILL.md` — PASS (1 match, line 79)
  - `grep -n "positive control" skills/shepherd/SKILL.md` — PASS (1 match, line 79)
  - `grep -n "DF-68" skills/shepherd/SKILL.md` — PASS (1 match, line 79)
  - `git diff skills/shepherd/SKILL.md` shows the WORKFLOW-VEHICLE-PROBE paragraph with ZERO removed lines, only new `+` lines immediately after it — PASS (diff shows only 2 added lines: the new paragraph plus its trailing blank line; the existing paragraph line is unchanged context, no `-` lines anywhere in the diff)
- Halts encountered: none
- Summary: Added one new paragraph, `PROBE-FALSIFIABILITY`, to `## Dispatch law` in `skills/shepherd/SKILL.md`, immediately after the byte-identical, untouched WORKFLOW-VEHICLE-PROBE paragraph and before `## Root contract`. The rule: any probe reported as a capability ABSENCE must state the positive control that would have shown presence (concrete evidence the instrument CAN return "present" under the right conditions) or it is not a finding and must route to an open question instead. Cites `DF-68` by name with a pointer to `.shepherd/runs/v645/dogfood.md` (no retelling of the tmux-socket/`claude -p` narrative — that stays in dogfood.md per the brief's doctrine-vs-narrative split) and ties the rule explicitly to the existing `@auditor` Hypothesis + Falsification + Confidence discipline (`agents/auditor.md §Per-finding contract`, backed by `superpowers:systematic-debugging`) as the analogue it generalizes one layer up, from a post-hoc audit finding to a lead's own capability probe. Verified via `rg`/`grep` that `PROBE-FALSIFIABILITY` did not already exist anywhere in the target file (zero DO-NOT-DUPLICATE hits) before writing. No other section of the file was touched (Root contract, Fan-out counterweight, Sprint contract, Operator surface, Principles, Invocation map all untouched); `commands/spawn.md` and `skills/harness/SKILL.md` were not opened for writing — those files showed as modified in `git status` only because sibling steps in this wave are editing them concurrently in the same worktree, confirmed disjoint from my write via `git diff skills/shepherd/SKILL.md` isolating exactly my paragraph.
- Reporter: coder-L7-S3 @ 2026-08-13T22:00:08Z

## INSIGHTS
- kind: gap — this dispatch arrived as flattened prose (CONTEXT/REQUIRED CHANGE/HARD CONSTRAINT/NON-GOALS/ACCEPTANCE) rather than the seven canonical bracketed headers (`[SKILLS]`/`[CONTEXT-INVENTORY]`/`[DO-NOT-DUPLICATE]`/`[USER-STYLE]`/`[FILE-SCOPE]`/`[NON-GOALS]`/`[ACCEPTANCE]`), and no plan/mesh/seed file in `.shepherd/runs/v645/` contained an `L7-S3` entry to cross-check against (confirmed via `grep -rn "L7-S3"` across the run directory — zero hits). The task was unambiguous enough to execute safely, but a stricter coder would have halted `BRIEF INVALID` on missing `[SKILLS]`/`[CONTEXT-INVENTORY]`/`[DO-NOT-DUPLICATE]`/`[WORKTREE]`/`[BASE-COMMIT-EXPECTED]` headers per Step 0 of this role's own protocol. Worth deciding explicitly whether direct-fix dispatches (like this DF-68 remediation, dispatched straight from root/dogfood rather than through the full engineer→plan→mesh pipeline) get a lighter-weight brief contract, and if so, document that exception in `skills/shepherd/references/flock.md §@coder` so future coders don't have to judgment-call it mid-dispatch.
