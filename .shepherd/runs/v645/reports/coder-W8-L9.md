## CODER REPORT

- Lane: W8-L9 (contradiction-ledger Findings #12 `VERSION-FILES-UNDOCUMENTED` + #19 `MOD-BASE-UNDOCUMENTED`)
- Skills loaded: code-style (markdown/doc-table conventions; no language-specific code body in scope)
- Files touched (created/modified/deleted): `/Users/jo3/src/fl03/shepherd/docs/configuration.md` (modified)
- LOC delta: +4 / -0 (2 new table rows + 1 explanatory prose paragraph + 1 blank separator line; doc-only, no `.py`/`.rs` production LOC — ONE-LOC rule not implicated)
- Acceptance grep results:
  - `rg -n "version_files" docs/configuration.md` → 2 hits (new table row line 180 + explanatory paragraph line 187) — PASS, key now documented
  - `rg -n "mod_base" docs/configuration.md` → 2 hits (new table row line 181 + explanatory paragraph line 187) — PASS, key now documented
  - `sed -n '33p' skills/shepherd/references/branching-model.md` → still cites `[branching.version_files]` at line 33; `sed -n '46p'` → still cites `[branching].mod_base` at line 46 — PASS, `[CONTEXT-INVENTORY]`-style citations verified live before writing, no drift
  - Column-shape check (`awk` pipe-count over lines 170-181) → every row in the `[branching]` table (existing 8 + 2 new) carries exactly 4 `|` chars (3 columns: Key/Type/Default) — PASS, new rows match the table's existing shape exactly; did NOT add a 4th "Meaning" column even though sibling tables in this file have one, because *this* table's established shape is 3-column
  - `git status --porcelain` scoped to my writes → only `docs/configuration.md` touched; `branching-model.md` left untouched per the ledger's own note ("`branching-model.md` edit not needed if the schema table is the corrected side") — PASS, FILE-SCOPE honored
- Halts encountered: none
- Summary: Verified both keys against the actual implementation before writing anything (per the brief's explicit instruction): `rg -n "version_files"` and `rg -n "mod_base"` across the whole repo (excluding worktrees) show the keys are read nowhere in code — `services/cli/shepherd_cli/commands/release.py`'s `VERSION_FILES` module constant hardcodes the five bumped files instead of reading `[branching].version_files`, and `_next_version()` hardcodes the mod-10 `< 9` rollover gears instead of reading `[branching].mod_base` — and the module's own header comment (release.py lines 25-28) already self-documents this as a known deferred gap ("the per-level `mod_base` overrides ... are NOT [wired] — same follow-up bash's own header comment defers"). Both keys are therefore doctrine-only/aspirational, not live config, and I documented them that way rather than as functioning schema entries: each new row's Default cell reads `*(not read)* — see note below`, and a new prose paragraph after the table (mirroring this same file's existing `harnesses`-key honesty pattern at lines 160-166) names the exact hardcoded substitutes and cites the deferral comment so a reader can verify the gap themselves. No compile-time confirmation needed from the central verifier — this is a pure markdown-doc addition with no code/build surface; the only thing worth a verifier double-check is that no sibling W8 lane also edited `docs/configuration.md`'s `[branching]` section concurrently (my `git status --porcelain` at write time showed only my file touched under that path, but other lanes were writing to other files in the same workspace concurrently per the wave's file-disjoint design).
- Reporter: coder-W8-L9 @ 2026-08-13T17:30:00Z
