# v6.1.5 Comprehensive-Changes Handoff — 2026-06-12

> Written for a fresh Opus[1m] session resuming the v6.1.5 work. Read this FIRST,
> then `CLAUDE.md`. The operator runs an axiom sprint in parallel as a live test bed.

## Where we are
- Branch **`v6.1.5`** (cut from v6.1.4). All 6 version-source files at `6.1.5`.
- **v6.1.4 is RELEASED to `main`** (`e8e4560 release: v6.1.4 (#144)`). Do not redo v6.1.4.
- v6.1.5 work is **pending the operator's commit** — the OPERATOR commits / tags / squashes / PRs. **Never push to main; PRs only.** Operator picks the version tier.
- Tests GREEN as of handoff: `bash hooks/tests/run.sh` → 37/37, `bash skills/context/tests/run.sh` → 40/40. **Run both after any script/hook change.**
- Pending uncommitted (operator will commit): `CHANGELOG.md` (v6.1.4 entry), `CLAUDE.md`, `docs/configuration.md`, `examples/minimal/shepherd.toml`, `hooks/scripts/{_lib,release_trigger_guard,tmux_pane_cleanup,coordinate_drive_guard,precompact_snapshot,focus_rehydrate,dispatch_guard,worktree_teardown_guard}.sh`, `skills/context/scripts/{_lib,cmd_release}.sh`, `skills/shepherd/doctrines/agent-excellence.md`.

## v6.1.4 SHIPPED (done — don't redo)
dev.10 release-trigger fix + `release_trigger_guard.sh` hook; `/loop` 3→7-day correction; loop discoverability (`SKILL.md §0-ter`, `[LOOP-CONTEXT]` in worker/discovery); `Workflow` glossary + "never ToolSearch" disambiguation (`references/glossary.md`); tmux observability (`shctx panes`) + `tmux_pane_cleanup.sh` (#66.6); `AskUserQuestion` + `doctrines/operator-signaling.md`.

## v6.1.5 DONE this session
- **#14 cfg_get** — config-resolution helper in BOTH `_lib.sh` (hooks + skills/context). Precedence **local → project → XDG**, per-key. 8 read-sites routed (the guards + cmd_release + quiet_warnings). **Use `cfg_get <key>` for ALL config reads — never hardcode `.claude/shepherd.toml` again.**
- **#11 subagent-preference** — extension to `agent-excellence.md` Rule 6 (delegate bulk work; orchestrators synthesize).

## v6.1.5 REMAINING — the comprehensive work (priority order)
Bounded first, large last:
1. **#8 seed-optional wiring** (small) — point `commands/start.md` (~L29) + `commands/spawn.md` (Check 4, ~L231) at the seedless flow in `doctrines/operator-signaling.md`. Doctrine already written; just wire the pointers. Seed = recommended, NOT required.
2. **#15 auto-scaffold config** (medium) — new `shctx config init`: scaffold `.claude/shepherd.toml` from `examples/minimal/shepherd.toml` when absent, deriving `name` (git remote/cwd) + `[gates]` (detect Cargo.toml→cargo, package.json→npm, pyproject→pytest, go.mod→go). start/spawn: scaffold→notice→PROCEED. plant: scaffold→ONE AskUserQuestion (replaces the hard #120 STOP). Write via `cfg_get`'s canonical location.
3. **#13 observability dashboard** (medium) — compose existing primitives (`shctx status`, `cmd_graph status`, `v_teammates_live`, FOCUS-LOOP) into a one-glance sprint dashboard; recommend a `/loop <interval> shctx <dash>` recipe in `spawn.md`. Don't build a new subsystem.
4. **#12 safe simplification** (medium) — execute the ~122 lines of behavior-neutral brief-dedup the survey found (below).
5. **#10 toggles** (LARGE, design depth) — all 4: operator pause/sign-off points, unattended grade gate (extends `[autorun].min_grade`), parallelism defaults, observability cadence. Read via `cfg_get`. **Perf-sound; defaults MUST preserve current behavior.**
6. **#9 axiom→neutral scrub** (LARGE, ~40 files) — make the plugin project-agnostic. Genericize `Field origin: axiom v0.3.x` provenance notes; **RENAME `examples/axiom/` → a neutral example (e.g. `examples/rust-service`)** + genericize contents (drop Polymarket/geo-block); genericize axiom config samples in `docs/{configuration,integration,customization}.md`; axiom-rt/axiom-node feature examples in `agents/{critic,coder}.md` + `critic.reference.md`; update README + CLAUDE.md. Leave `CHANGELOG.md` (historical). Verify with `rg -ni axiom`.

## Binding constraints (CRITICAL — these caused regressions this session)
- **bash 3.2** (macOS /bin/bash): NO `${,,}`/`${^^}`, `mapfile`/`readarray`, `declare -A`. Guard or use `tr`/`awk`/`grep -oE`.
- **`set -e` + `pipefail`**: a failing command-substitution pipeline KILLS the script. Append `|| true` to `VAR="$(grep ... | ...)"`. (This regressed `cmd_release.sh`; tests caught it.)
- **PERF/SOUNDNESS** (operator flagged recent perf regressions): config reads behind cheap fast-paths (e.g. `release_trigger_guard` raw-greps for `dev.N` BEFORE spawning jq); defaults preserve behavior; never add hot-path cost. Be logical and sound.
- **test_exec_bits**: new path-invoked scripts need `chmod +x` AND git mode `100755` (`git add` records it).
- **operator-signaling**: planter asks freely; execution sessions (start/spawn/root) are ACTION-BIASED — `AskUserQuestion` ONLY for no-seed kickoff / irreversible outward actions / hard blocking forks. NEVER confirmation/approval-seeking or new mid-run stop points.
- **Plugin is WELL-FACTORED** — simplify only via shared-reference extraction; don't over-refactor or merge separated doctrines.

## Survey findings (done this session — DON'T re-run the surveys)
- **Doctrines (60):** well-factored, zero redundant. Optional deferred: a `combo-wave-patterns.md` parent for intro/discovery combo-waves (~60 lines).
- **Briefs/scripts — ~122 lines SAFE dedup (#12):** specialist-dispatch rule → one shared reference (~22L, conductor+shepherd); "flock-first" dup in conductor (~8L); halt-code tables → `references/halt-codes.md` (~60L across conductor/shepherd/planter); mode-table explanation compression (~8L, medium-risk); "NEVER write source code" → shared base + per-role exceptions (~24L, medium-risk). Scripts: NO redundancy (`cmd_status`/`cmd_sync` are live). Config: no dead keys.

## Reference
- **Issue #146** (open): future — auto-adapt to available CC plugins (`/remember`, `superpowers`) via the toolkit/adaptation loop. Not v6.1.5.
- **Context7** has current CC docs — use it for `Workflow` tool, `/loop` (7-day expiry), `teammateMode`, hook events. `/websites/code_claude` (benchmark 94.4) is the best source.
- Session origin: started as "tmux routines fell off + dev.10 release bug," expanded into the full v6.1.4 release + v6.1.5 foundations.
