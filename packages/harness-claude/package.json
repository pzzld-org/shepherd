{
  "name": "@fl03/harness-claude",
  "version": "6.4.5",
  "private": true,
  "description": "Claude Code adapter: consumes @fl03/compiler's `claude` emission target (agents/*.md, skills/*/SKILL.md; content/ has no commands/ or hooks.json source yet, see README) and resolves it onto Claude's concrete mechanisms -- model_hint -> model:, a PreToolUse guard relay to the shared Rust predicate engine, and declared dispatch ceilings. Implemented in Wave 4 (W4-S4).",
  "type": "module",
  "scripts": {
    "test": "node test.mjs"
  },
  "dependencies": {
    "@fl03/compiler": "6.4.5"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
